apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: dev-platform-minio
  namespace: argocd
spec:
  description: "Platform minio (dev)"
  sourceRepos:
    - '*'
  destinations:
    - namespace: '*'
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
