apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: dev-platform-argo-workflows
  namespace: argocd
spec:
  description: "Platform argo-workflows (dev)"
  sourceRepos:
    - '*'
  destinations:
    - namespace: '*'
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
