apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: dev-platform-kong
  namespace: argocd
spec:
  description: "Platform kong (dev)"
  sourceRepos:
    - '*'
  destinations:
    - namespace: '*'
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
