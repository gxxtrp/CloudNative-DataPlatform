apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-platform-vault
  namespace: argocd
  labels:
    env: dev
    tier: platform
    service: vault
spec:
  project: dev-platform-vault
  source:
    repoURL: https://github.com/example/data-platform.git
    targetRevision: HEAD
    path: k8s/platform/vault/overlays/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: platform
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
