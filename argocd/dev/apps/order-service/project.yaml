apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: dev-app-order-service
  namespace: argocd
spec:
  description: "Application order-service (dev)"
  sourceRepos:
    - '*'
  destinations:
    - namespace: apps
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
