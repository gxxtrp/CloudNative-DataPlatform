apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-app-stream-ingestor
  namespace: argocd
  labels:
    env: dev
    tier: workload
    app: stream-ingestor
spec:
  project: dev-app-stream-ingestor
  source:
    repoURL: https://github.com/example/data-platform.git
    targetRevision: HEAD
    path: k8s/apps/stream-ingestor/overlays/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: apps
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
