apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: dev-app-rider-service
  namespace: argocd
spec:
  description: "Application rider-service (dev)"
  sourceRepos:
    - '*'
  destinations:
    - namespace: apps
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
