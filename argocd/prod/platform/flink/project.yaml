apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: prod-platform-flink
  namespace: argocd
spec:
  description: "Platform flink (prod)"
  sourceRepos:
    - '*'
  destinations:
    - namespace: '*'
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
