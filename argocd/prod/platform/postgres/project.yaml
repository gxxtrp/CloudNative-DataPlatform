apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: prod-platform-postgres
  namespace: argocd
spec:
  description: "Platform postgres (prod)"
  sourceRepos:
    - '*'
  destinations:
    - namespace: '*'
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
