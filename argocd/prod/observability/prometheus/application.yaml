apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: prod-observability-prometheus
  namespace: argocd
  labels:
    env: prod
    tier: observability
    service: prometheus
spec:
  project: prod-observability-prometheus
  source:
    repoURL: https://github.com/example/data-platform.git
    targetRevision: HEAD
    path: k8s/observability/prometheus/overlays/prod
  destination:
    server: https://kubernetes.default.svc
    namespace: observability
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
