apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: prod-app-stream-ingestor
  namespace: argocd
spec:
  description: "Application stream-ingestor (prod)"
  sourceRepos:
    - '*'
  destinations:
    - namespace: apps
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
