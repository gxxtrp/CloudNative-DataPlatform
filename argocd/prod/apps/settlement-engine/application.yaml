apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: prod-app-settlement-engine
  namespace: argocd
  labels:
    env: prod
    tier: workload
    app: settlement-engine
spec:
  project: prod-app-settlement-engine
  source:
    repoURL: https://github.com/example/data-platform.git
    targetRevision: HEAD
    path: k8s/apps/settlement-engine/overlays/prod
  destination:
    server: https://kubernetes.default.svc
    namespace: apps
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
