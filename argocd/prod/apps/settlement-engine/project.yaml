apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: prod-app-settlement-engine
  namespace: argocd
spec:
  description: "Application settlement-engine (prod)"
  sourceRepos:
    - '*'
  destinations:
    - namespace: apps
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
