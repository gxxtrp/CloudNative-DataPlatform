apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data-pvc
  namespace: platform
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-isolated
  resources:
    requests:
      storage: 5Gi
