apiVersion: v1
kind: Service
metadata:
  name: stream-ingestor
  namespace: apps
  labels:
    app: stream-ingestor
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8082
      targetPort: 8082
  selector:
    app: stream-ingestor
